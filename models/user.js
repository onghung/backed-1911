class User {
    constructor(account, active, firstName, lastName) {
        this.account = account;
        this.active = active;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}

module.exports = User;